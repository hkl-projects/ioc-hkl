# RELEASE - Location of external support modules
#
# IF YOU CHANGE ANY PATHS in this file or make API changes to
# any modules it refers to, you should do a "make rebuild" in
# this application's top level directory.
#
# Host- or target-specific paths: configure/RELEASE.local (not in git).
# Copy configure/RELEASE.local.example and set MODULES / EPICS_BASE for your site.
#
# This file is parsed by both GNUmake and an EPICS Perl script,
# so it may ONLY contain definitions of paths to other support
# modules, variable definitions that are used in module paths,
# and include statements that pull in other RELEASE files.

# ── Default dev paths (override in RELEASE.local) ─────────────────────────

MODULES = /epics/support

PVXS = $(MODULES)/pvxs

EPICS_BASE = /epics/base

TEMPLATE_TOP = $(EPICS_BASE)/templates/makeBaseApp/top

# Pixi Python for PyDevice / hkl bindings (recommended)
PYTHON_CONFIG = $(TOP)/.pixi/envs/default/bin/python3.12-config

# System Python only when building hkl from source (run_HKL.sh option 2)
#PYTHON_CONFIG = /usr/bin/python3-config

# ── Local overrides (required at most facility sites) ───────────────────
-include $(TOP)/../RELEASE.local
-include $(TOP)/../RELEASE.$(EPICS_HOST_ARCH).local
-include $(TOP)/configure/RELEASE.local
